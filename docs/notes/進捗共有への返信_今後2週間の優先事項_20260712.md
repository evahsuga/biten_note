# 進捗共有への返信：今後2週間の優先事項について

**宛先**: Claude Code 実装セッション
**作成**: claude.ai（壁打ちパートナー）
**日付**: 2026年7月12日
**背景**: 「進捗共有_安心利用協力利用_20260712.md」を受けての、壁打ちチャットでの検討結果

---

## 0. 結論（3行サマリ）

- **今回の2週間は、既存の設計仕様書（フッターリンク版）＋差分1（お知らせバッジ機能）のスコープに集中**する。5章で報告のあった「Firebase脱却・完全ローカル化」の大方針は、**方向性としては合意したが、今回は着手しない**（詳細は3章）。
- 2週間の焦点は一言でいうと、**「新規最大100名程度の方に、手元の端末だけで完結する環境を、ボタン操作で迷わせず提供すること」**。
- 直近でやるべきことを優先順位付きで4章にまとめた。特に①（Firestore書き込みゼロの検証）が最優先。

---

## 1. 大方針（Firebase脱却・完全ローカル化）について

進捗報告5章の「ローカル完結主体への全面移行」は、方針としては合意する。特に「既存ユーザーへの救済措置として、データ移行ボタン＋LINEでのリマインダー（メール通知の代替）」という方向性は良いと考えている。

**ただし、今回の2週間でこれを進めることはしない。** 理由は、以前の壁打ちで暗号化実装を見送った時と同じ判断軸——「時間に追われて慣れない作業・慎重性を要する作業を行うのは推奨しない」——による。

- 移行スクリプト・移行ボタンのUX設計・協力利用の段階的サンセットは、**講習会後、落ち着いてから着手**する
- `docs/BACKLOG.md`・仕様書11章への記録はこのままでよいが、**今回のスプリントのタスクリストには含めない**でほしい

---

## 2. 今回の2週間のスコープ再確認

進捗報告0章・4章の内容と概ね一致しているが、念のため再確認する。

- **含む**：安心利用主体のログイン画面（フッターリンク版）、お知らせバッジ機能（差分1）
- **含まない**：合言葉方式の暗号化、Firebase脱却・完全ローカル化、LINEリマインダー機能の実装

---

## 3. 本番（Netlify）昇格の切り分け方針

v1.9→v3.0への昇格時、以下の2点で本番投入の範囲を限定する。

### 3.1 Cloud Functions（メール送信）は今回デプロイしない

- フロントエンドの昇格（`git merge` → Netlify）と、Cloud Functionsのデプロイ（`firebase deploy --only functions`）は別作業のため、**今回は後者を実行しない**
- 既存のGitHub Pages（開発環境）で使い続けている既存テスト協力者には影響なし（そのまま利用可能）
- 気にされそうな既存テスト協力者には、LINEへの移行予定を個別に一声かけておく（運用対応、コード変更ではない）

### 3.2 通知設定UI（v3.0）は本番でのみ非表示にする

- v3.0の画面には、すでに通知設定（頻度・時刻設定UI）が協力利用ユーザー向けに見える形で実装済みとのこと
- **GitHub Pages（開発環境）ではそのまま表示**（既存テスト協力者向けに現状維持）
- **Netlify（本番）でのみ非表示**にする
- ⚠️ 開発・本番は同一コード・同一Firebaseプロジェクトを共有しているため、ビルドを分けて対応することはできない。**アクセス元のドメイン判定（例：`window.location.hostname === 'bitennote.netlify.app'` かどうか）で表示・非表示を切り替える実装**が必要になる。この対応漏れがないよう確認をお願いしたい

---

## 4. 優先順位（今回の2週間）

| 優先度 | 内容 | 備考 |
|---|---|---|
| **最優先** | 安心利用でFirestoreへの書き込みが一切発生しないことの検証（BACKLOG #8） | 「本人以外は誰も見られない」という説明の前提そのもの。100名への説明前に必ず完了させる |
| 高 | 本番昇格時、Cloud Functions（メール送信）はデプロイしない | 3.1参照 |
| 高 | 本番でのみ通知設定UIを非表示にする（ドメイン判定） | 3.2参照 |
| 中 | 協力利用の同意チェックボックス画面の実装状況を確認し、未実装なら追加 | 仕様書§8チェックリストで「要確認」となっていた点 |
| 中 | お知らせバッジ機能（差分1）の実装 | 内容はそのまま |
| 低 | 既存ユーザーの回帰テスト、協力利用の登録フロー動作確認 | 本番昇格前に実施 |

---

## 5. 参照ファイル

- `docs/specs/安心利用_協力利用_ログイン設計仕様書.md`（フッターリンク版・実装のベース）
- `docs/specs/安心利用_協力利用_ログイン設計仕様書_差分1_お知らせバッジ機能.md`
- `docs/notes/紹介の仕組みづくり_壁打ち記録_20260711_続編.md`
- `docs/notes/紹介の仕組みづくり_壁打ち記録_20260711_続編_差分1.md`
- `docs/BACKLOG.md`（大方針・#8・#10等の記録先）
