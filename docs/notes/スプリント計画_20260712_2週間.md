# スプリント計画（2週間）— 2026-07-12 起点

**背景**: 設計側（claude.ai）からの返信「進捗共有への返信_今後2週間の優先事項_20260712」を受けて、今回2週間の作業優先事項を再整理したもの。
**ゴール（設計側の言葉）**: 「新規最大100名程度の方に、手元の端末だけで完結する環境を、ボタン操作で迷わせず提供する」
**講習会の目安**: 2026-07-25 頃（この計画の期限感）

---

## スコープ確定

- **含む**: 安心利用主体のログイン画面（フッターリンク版・実装済みの仕上げ）／お知らせバッジ機能（差分1）／本番昇格の切り分け対応
- **含まない（今回やらない）**: 合言葉暗号化／**Firebase脱却・完全ローカル化（移行ボタン）**／LINEリマインダー実装
  - → Firebase脱却の大方針は**合意済みだが、講習会後に着手**。記録（仕様書11章・BACKLOG #10）は残すが、**今回のタスクには含めない**（設計側の明示指示。理由は「期限に追われて慎重を要する作業をしない」＝暗号化見送り時と同じ判断軸）。

---

## 優先順位（今回2週間）

| 優先 | タスク | repo実態・着手ポイント | 種別 |
|---|---|---|---|
| ~~**P0 最優先**~~ ✅完了 | **安心利用でFirestore書き込みゼロを検証（BACKLOG #8）** | **2026-07-12 完了**。静的監査で3層防御＝漏れ経路なしを確認。第4層ガードは見送り、不変条件を `CLAUDE.md` に明記。Console実測は方針により省略。記録: `docs/notes/検証記録_安心利用Firestore非書込_20260712.md` | 検証 |
| P1 高 | 本番昇格時、Cloud Functions（メール送信）を**デプロイしない** | フロント昇格（`git merge`→Netlify）と `firebase deploy --only functions` は別作業。**後者を実行しないだけ**。コード変更なし・デプロイ規律 | 運用 |
| P1 高 | 本番でのみ通知設定UIを非表示（**ドメイン判定**） | `window.location.hostname === 'bitennote.netlify.app'` で分岐。対象は少なくとも ①`#/notification-settings` ルート(app.js:247) ②「🔔 リマインダー設定」ボタン(app.js:1982) ③ホームお知らせカードの「リマインダー設定へ→」(app.js:2663)。**GitHub Pages（開発）では現状表示のまま**。既存hostname判定は未実装＝新規 | 実装 |
| P2 中 | 協力利用の同意チェックボックスの実装状況を精査し、未実装なら追加 | 新規登録フローは `auth.js:146` (`createUserWithEmailAndPassword`)。**現状、登録を1ステップ止める専用の同意チェック画面は見当たらない**（プライバシー/規約の2モード文言は実装済み）。仕様書4章のドラフトに沿って同意ステップを追加するか判断 | 実装/判断 |
| P2 中 | お知らせバッジ機能（差分1）の実装 | 新規 `js/announcements.js`（id/date/title/body/linkUrl?/linkLabel?）／既読保存: 安心利用=IndexedDB・協力利用=`users/{uid}/settings/appSettings`／「使い方」付近に未読数の丸数字バッジ（0件は非表示）／一覧を開いた時点でまとめて既読。**中身（初回お知らせ本文）は今回スコープ外＝仕組みのみ** | 実装 |
| P3 低 | 既存ユーザーの回帰テスト・協力利用の登録フロー動作確認 | 本番昇格前に実施 | 検証 |

---

## 本番（Netlify）昇格の切り分けルール（設計側3章）

v1.9 → v3.0 昇格時、本番投入の範囲を限定する：

1. **Cloud Functions（メール送信）は今回デプロイしない** … `firebase deploy --only functions` を実行しない。既存テスト協力者は GitHub Pages（開発）でそのまま利用可。移行予定は個別に一声（運用対応）。
2. **通知設定UI（v3.0）は本番でのみ非表示** … 同一コード・同一Firebase・ビルド分離不可のため、**hostname 判定で表示/非表示を切替**（上表 P1）。GitHub Pages では表示維持。

> ⚠️ 昇格すると v1.9→v3.0 に一気に上がり、v2.0（メール通知）も本番初投入になる。上記2点の切り分け漏れがないこと。詳細は `docs/DEPLOYMENT_NOTES.md`。

---

## 今回スコープ外（講習会後に再開）

- Firebase脱却・完全ローカル化／Firestore→ローカル移行スクリプト＋移行ボタン（BACKLOG #10・仕様書11章）
- LINEリマインダー（メール通知の代替）
- 合言葉暗号化
- BACKLOG #1（Netlify設定リポジトリ外・残置）/ #3 App Check / #4 写真base64 / #5 オフライン複数タブ / #9 Firebase使用量 … いずれも今回は着手しない（#3/#9は方針転換で緊急性消滅済み）

---

## 参照

- 設計側返信: `進捗共有への返信_今後2週間の優先事項_20260712.md`（Downloads。必要なら docs/notes へ格納）
- ベース仕様: `docs/specs/安心利用_協力利用_ログイン設計仕様書.md`
- 差分1: `docs/specs/安心利用_協力利用_ログイン設計仕様書_差分1_お知らせバッジ機能.md`
- 進捗共有（前便）: `docs/notes/進捗共有_安心利用協力利用_20260712.md`
- 課題台帳: `docs/BACKLOG.md`
